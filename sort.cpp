#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    for(int i=0;i<n;i++)
    {
            cout<<"Hi";
    }
    vector<int> array = {5, 3, 2, 4, 1};
    sort(array.begin(), array.end());
    printArray(array);

    return 0;
}